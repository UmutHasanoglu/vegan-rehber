# ============================================
# FRONTEND VARIABLES (VITE_* prefix)
# ============================================

# Published CSV URL for public reads (no auth required)
# To get this: File > Share > Publish to web > Select "Comma-separated values (.csv)" > Publish
VITE_GOOGLE_SHEET_CSV_URL=https://docs.google.com/spreadsheets/d/your_sheet_id/export?format=csv&gid=0

# Admin PIN for dashboard access (4-6 digits recommended)
# This is checked client-side for login, and server-side for API calls
VITE_ADMIN_PIN=1234

# ============================================
# NETLIFY FUNCTION VARIABLES (server-side only)
# ============================================

# Google Sheet ID (from the URL)
# https://docs.google.com/spreadsheets/d/[SHEET_ID]/edit
GOOGLE_SHEET_ID=your_google_sheet_id_here

# Service Account Email
# From your service account JSON file (client_email field)
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com

# Service Account Private Key
# From your service account JSON file (private_key field)
# IMPORTANT: Copy the entire key including -----BEGIN PRIVATE KEY----- and -----END PRIVATE KEY-----
# In Netlify, paste it as-is with the newlines
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# Admin PIN for API authentication (must match VITE_ADMIN_PIN)
ADMIN_PIN=1234
